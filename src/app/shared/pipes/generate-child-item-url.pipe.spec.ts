import { GenerateChildItemUrlPipe } from './generate-child-item-url.pipe';

describe('GenerateChildItemUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateChildItemUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
