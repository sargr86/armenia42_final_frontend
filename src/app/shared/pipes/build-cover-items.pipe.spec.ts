import { BuildCoverItemsPipe } from './build-cover-items.pipe';

describe('BuildCoverItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildCoverItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
