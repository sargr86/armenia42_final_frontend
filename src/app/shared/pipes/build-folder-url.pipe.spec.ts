import { BuildFolderUrlPipe } from './build-folder-url.pipe';

describe('BuildFolderUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildFolderUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
